import { NativeStackScreenProps } from '@react-navigation/native-stack'
import { LookForScreenParamList, Screens } from '../../navigation/types'

export type LookForScreenProps = NativeStackScreenProps<
    LookForScreenParamList,
    Screens.LookForScreen
>
